# --- Python base (already in GitHub's template, keeping here for clarity)
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
ENV/
env/
pip-wheel-metadata/
.pytest_cache/
.pyre/
.mypy_cache/

# --- Editors/OS
.DS_Store
.idea/
.vscode/

# --- Secrets / env
.env
*.env
.env.*.local

# --- Local data (don’t commit your deals)
data/
!data/.gitkeep

# If you run with DATA_DIR=/data on Railway, nothing to ignore here (container FS).
# If you ever keep CSVs elsewhere locally, ignore them too:
*.csv
*.xlsx
*.zip
